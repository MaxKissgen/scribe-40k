# Copy to .env and fill in whichever provider you actually use.
# Only the key for your configured provider is needed.

MISTRAL_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
OPENROUTER_API_KEY=
AZURE_OPENAI_API_KEY=

# Override where runtime state and caches live (both default to the repo root).
# SCRIBE40K_DATA=./data
# SCRIBE40K_CACHE=./.cache
